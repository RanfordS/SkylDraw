#include "core.h"

extern size_t rm_factorial (size_t n);
extern size_t rm_choose (size_t n, size_t k);
extern float rm_sign (float a);
extern bool quadraticSolve (float a, float b, float c, float res[2]);
